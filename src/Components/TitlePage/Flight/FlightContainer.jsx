import React from "react";
import { connect } from "react-redux";

class FlightContainer extends React.Component {
  render() {
    <Flight />;
  }
}

export default connect(mapStateToProps, null)(FlightContainer);
